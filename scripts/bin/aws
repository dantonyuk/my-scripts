#!/bin/zsh

if [ -z ${AWS_PROFILE} ]
then
    source aws-profile
fi

AWS_COMMAND=${AWS_COMMAND:-/opt/homebrew/bin/aws}

if command -v aws-vault &> /dev/null
then
  aws-vault exec ${AWS_PROFILE} -- ${AWS_COMMAND} $@
else
  ${AWS_COMMAND} $@
fi
